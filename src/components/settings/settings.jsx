import classes from './settings.module.css'

const Settings = () => {
    return(
        <div className={classes.settings}>
            <h2 className={classes.settings__heading}>Settings</h2>
        </div>
    );
};

export default Settings;